package IQns;

// Find dollars spent for server uptime
// $1 per minute of server uptime from its start time to end time.
// If the end time for the server is "None", means it's still running
// In that case we have to consider the currTime as end time.
// Constraints :
// startTime <= endTime <= currTime || endTime == "None"
public class FindDollarsFromServerUptimeToCurrTimeOrEndTime {

    public int findTotalDollarsSpent(String[] startTime, String[] stopTime, String currTime) {
        return 0;
    }

    public static void main(String[] args) {

    }

}
